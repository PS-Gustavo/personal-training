#include <iostream>

string decompressString(Vector<string>& compressedString) {
    
}

int main() {

    std::Vector<string> = compressedString;

    std::cout << decompressString(compressedString) << endl;
    return 0;
}